#Saber si hay cambios
git status

#Seleccionar los archivos a subir eventualmente
git add <archivo>

#Subir al sistema
git commit -m <mensaje>

#Subir lo anterior
git push origin master

#Baja lo del repositorio
git pull origin master